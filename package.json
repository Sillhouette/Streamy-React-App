{
  "name": "Streamy",
  "engines": {
    "node": "10.3.0"
  },
  "scripts": {
    "build": "cd client && npm install && npm run build && cd rtmpserver && npm install && npm run build && cd .. && cd ..",
    "deploy": "cp -a client/build/. public/ && cp -a client/rtmpserver/build/. public/",
    "postinstall": "npm run build && npm run deploy && echo 'Client built!'"
  }
}
